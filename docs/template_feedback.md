# Cookiecutter Template Feedback

> **Project**: rag-processor
> **Template**: [cookiecutter-python-template](https://github.com/ByronWilliamsCPA/cookiecutter-python-template)
> **Date**: 2025-12-05

## Issues Discovered During Project Setup

### 1. ❌ CRITICAL: Syntax Error in Sentry Integration Template

**File**: `{{cookiecutter.project_slug}}/src/{{cookiecutter.package_name}}/core/sentry.py`
**Line**: 74
**Severity**: Critical (breaks CI/CD)

**Issue**:

```python
try:
    import sentry_sdk
    from sentry_sdk.integrations.fastapi import FastApiIntegration
    from sentry_sdk.integrations.starlette import StarletteIntegration
    from sentry_sdk.integrations.logging import LoggingIntegration
    from sentry_sdk.integrations.sqlalchemy import SqlalchemyIntegration
    except ImportError:  # ❌ WRONG INDENTATION
```

**Expected**:

```python
try:
    import sentry_sdk
    from sentry_sdk.integrations.fastapi import FastApiIntegration
    from sentry_sdk.integrations.starlette import StarletteIntegration
    from sentry_sdk.integrations.logging import LoggingIntegration
    from sentry_sdk.integrations.sqlalchemy import SqlalchemyIntegration
except ImportError:  # ✅ CORRECT INDENTATION
```

**Impact**:

- Ruff format fails with "Expected a statement" error
- CI pipeline fails on Code Quality Checks job
- Project cannot pass pre-commit hooks
- Blocks initial setup for all new projects

**Context**:
Discovered during `/plan` command execution when CI checks failed on planning documents PR (#1).

**Suggested Fix**:

1. Fix indentation in template file
2. Add test to cookiecutter CI that validates Python syntax of generated files
3. Run `ruff check` on generated template output in CI

---

### 2. ⚠️ HIGH: Pre-Commit Config YAML Syntax Error

**File**: `.pre-commit-config.yaml`
**Line**: 66
**Severity**: High (breaks pre-commit hooks)

**Issue**:

```yaml
entry: bash -c 'command -v trufflehog >/dev/null 2>&1 && trufflehog git file://. --since-commit HEAD --results=verified,unknown --fail || echo "TruffleHog not installed - skipping (install: brew install trufflehog)"'
```

The single-quoted string contains unescaped single quotes in the parenthetical, causing YAML parsing to fail.

**Expected**:

```yaml
entry: bash -c 'command -v trufflehog >/dev/null 2>&1 && trufflehog git file://. --since-commit HEAD --results=verified,unknown --fail || echo "TruffleHog not installed - skipping"'
```

**Impact**:

- Pre-commit hooks fail to run
- `git commit` fails with YAML parsing error
- Workaround requires `--no-verify` (defeats security scanning)

**Context**:
Discovered when attempting to commit planning documents. Required manual fix before successful commit.

**Suggested Fix**:

1. Remove parenthetical from echo message OR escape properly
2. Add YAML linting to cookiecutter CI
3. Test pre-commit config in generated project during CI

---

### 3. 📝 MEDIUM: Front Matter Validation Hook Too Strict for Planning Documents

**File**: `.pre-commit-config.yaml`
**Hook**: `validate-front-matter`
**Severity**: Medium (blocks planning document commits)

**Issue**:
The front matter validation hook requires all markdown files to have strict YAML front matter matching specific schemas. However, planning documents generated by `/plan` command naturally don't have this front matter initially.

**Errors Encountered**:

```text
docs/planning/project-vision.md: ISSUES
  - redundant H1 found: '# Project Vision & Scope...' — remove it; 'title' renders automatically
  - : Unable to extract tag using discriminator 'schema_type'

docs/planning/adr/adr-001-initial-architecture.md: ISSUES
  - redundant H1 found: '# ADR-001: ...' — remove it; 'title' renders automatically
  - : Unable to extract tag using discriminator 'schema_type'
```

**Impact**:

- Planning documents cannot be committed without `--no-verify`
- Defeats purpose of having planning document templates
- Inconsistent with `/plan` command workflow

**Context**:
All planning documents (project-vision.md, tech-spec.md, roadmap.md, ADRs) failed front matter validation even though they follow standard markdown structure.

**Suggested Fix Options**:

**Option 1** (Recommended): Exclude planning documents from strict front matter validation

```yaml
- id: validate-front-matter
  files: ^docs/(?!planning/).*\.md$  # Exclude docs/planning/
```

**Option 2**: Make planning document schemas optional

```python
# In validation script
if file_path.startswith('docs/planning/'):
    schema_required = False
```

**Option 3**: Auto-generate front matter in `/plan` command

- Update `.claude/skills/project-planning/` templates to include minimal front matter
- Ensure `schema_type: planning` is added to all generated docs

---

### 4. 📄 LOW: Documentation Clarity - Planning Document Workflow

**Severity**: Low (documentation improvement)

**Issue**:
The `docs/planning/` directory README states planning documents are "Awaiting Generation" but doesn't clearly explain the relationship between:

1. The `/plan` command (generates comprehensive documents)
2. The template placeholders (minimal scaffolds)
3. The front matter validation requirements

**Impact**:

- User confusion about which documents to generate vs. manually write
- Unclear whether to use `/plan` command or write manually
- Front matter validation failures seem unexpected

**Suggested Fix**:
Update `docs/planning/README.md` with clear workflow:

```markdown
## Generation Workflow

### Option 1: Claude Code `/plan` Command (Recommended)
1. Run `/plan <project description>` in Claude Code
2. Documents generated without front matter (validation excluded)
3. Comprehensive planning documents ready for implementation

### Option 2: Manual Creation
1. Copy templates from `.claude/skills/project-planning/templates/`
2. Add front matter header (required for validation)
3. Fill in project-specific content

**Note**: Planning documents generated by `/plan` are excluded from strict
front matter validation to support rapid iteration.
```

---

### 5. 🔧 LOW: ClusterFuzzLite Fails on Empty Project

**Job**: ClusterFuzzLite (address)
**Severity**: Low (expected for new project)

**Issue**:
ClusterFuzzLite fuzzing job fails on newly generated projects because there are no fuzz targets yet.

**Impact**:

- Red CI check on first PR (cosmetic, but confusing)
- Suggests CI failure when project is actually fine

**Suggested Fix**:
Add condition to skip fuzzing on empty projects:

```yaml
- name: Run ClusterFuzzLite
  if: hashFiles('fuzz/*.py') != ''
  # ... existing config
```

Or add placeholder fuzz target in template:

```python
# fuzz/example_fuzz.py
"""Placeholder fuzz target - replace with actual targets."""
def TestOneInput(data: bytes) -> None:
    pass
```

---

### 6. 📋 LOW: Dependency Review Fails on Planning-Only PRs

**Job**: Dependency Review
**Severity**: Low (expected for docs-only changes)

**Issue**:
Dependency Review workflow fails when PR contains only documentation changes (no dependency changes).

**Impact**:

- Red CI check on documentation PRs
- Creates noise in CI status

**Suggested Fix**:
Add path filter to dependency review workflow:

```yaml
on:
  pull_request:
    paths:
      - 'pyproject.toml'
      - 'uv.lock'
      - 'requirements*.txt'
```

---

### 7. 📊 LOW: SonarCloud Analysis Fails on Empty Codebase

**Job**: SonarCloud Analysis
**Severity**: Low (expected for new project without code)

**Issue**:
SonarCloud analysis fails when there's no Python code to analyze (planning documents PR).

**Impact**:

- Red CI check before writing code
- Cannot merge docs-only PRs without forcing

**Suggested Fix**:
Add condition to skip on docs-only changes:

```yaml
- name: SonarCloud Scan
  if: hashFiles('src/**/*.py', 'tests/**/*.py') != ''
  # ... existing config
```

---

### 8. ❌ CRITICAL: GitHub Actions Workflow JSON Output Format Error

**File**: `.github/workflows/python-compatibility.yml`
**Severity**: Critical (breaks CI matrix)

**Issue**:

The Python Compatibility Matrix workflow fails with "Invalid format" error when setting matrix output:

```text
##[error]Unable to process file command 'output' successfully.
##[error]Invalid format '  "python": ['
```

**Root Cause**:
The workflow attempts to set a JSON matrix as output, but the format is incorrect for GitHub Actions workflow syntax.

**Impact**:

- Python Compatibility Matrix job fails on all PRs
- Matrix builds cannot run
- CI is red even when code is valid
- Blocks PR merges requiring all checks to pass

**Context**:
Discovered on PR #1 during latest CI run (19974609703).

**Suggested Fix**:
Review the matrix output formatting in the workflow. GitHub Actions requires specific JSON encoding for matrix outputs:

```yaml
# Correct format
- id: set-matrix
  run: |
    echo "matrix={\"python\":[\"3.11\",\"3.12\",\"3.13\"]}" >> $GITHUB_OUTPUT
```

---

### 9. ❌ HIGH: setup-uv Action Download Failure

**Workflow**: Multiple workflows using `astral-sh/setup-uv`
**Severity**: High (intermittent CI failures)

**Issue**:

```text
##[error]An action could not be found at the URI
'https://api.github.com/repos/astral-sh/setup-uv/tarball/582b2d78...'
```

**Root Cause**:
The workflow references a specific commit SHA of `astral-sh/setup-uv` that may have been force-pushed or the GitHub API is experiencing issues.

**Impact**:

- FIPS Compliance Check fails
- CI Pipeline fails
- Intermittent failures even when code is correct

**Suggested Fix**:

**Option 1** (Recommended): Use tagged version instead of commit SHA

```yaml
- uses: astral-sh/setup-uv@v4  # Use tag instead of commit
```

**Option 2**: Update to latest commit SHA

```bash
# Get latest commit
gh api repos/astral-sh/setup-uv/commits/main --jq '.sha'
```

**Option 3**: Pin to known-good commit with fallback

```yaml
- uses: astral-sh/setup-uv@582b2d78a0f5913301dcc87c4e93301fdd2b6711
  continue-on-error: true  # Don't fail PR for action download issues
```

---

## Summary of Recommendations for Template Team

### Immediate Fixes (Blockers)

1. ✅ Fix sentry.py indentation syntax error
2. ✅ Fix pre-commit config YAML escaping
3. ❌ Fix Python Compatibility Matrix JSON output format
4. ❌ Update setup-uv action to use tag instead of commit SHA

### High Priority

1. Exclude planning docs from front matter validation OR auto-generate front matter
2. Add syntax validation to template CI (run ruff/mypy on generated output)

### Nice to Have

1. Skip ClusterFuzzLite on empty projects
2. Skip Dependency Review on docs-only PRs
3. Skip SonarCloud on code-free PRs
4. Improve planning document generation workflow documentation

---

## Testing Recommendations

To prevent similar issues in future template updates:

```bash
# In cookiecutter-python-template CI
- Generate project from template
- Run all pre-commit hooks on generated project
- Run Ruff format/check on generated code
- Validate YAML files (including .pre-commit-config.yaml)
- Test planning document generation workflow
- Ensure CI passes on freshly generated project
```

---

**Reporter**: Claude Code (via `/plan` command)
**Contact**: Byron Williams <byron@williamscpa.dev>
